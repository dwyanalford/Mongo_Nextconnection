import '../styles/global.css';
import '../styles/output.css';

// This default export is required in a new `pages/_app.js` file.
export default function TidalApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
